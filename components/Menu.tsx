'use client';
import { useState } from 'react'; import { menu } from '@/data/site'; import Reveal from './Reveal';
const categories = Object.keys(menu) as (keyof typeof menu)[];
export default function Menu() { const [active, setActive] = useState<(keyof typeof menu)>('Coffee'); return <section id="menu" className="section"><div className="container"><Reveal><div className="section-head"><p className="eyebrow">The menu</p><h2>Good things, <em>well made.</em></h2></div></Reveal><div className="menu-tabs" role="tablist">{categories.map(category => <button role="tab" aria-selected={active === category} className={active === category ? 'active' : ''} onClick={() => setActive(category)} key={category}>{category}</button>)}</div><div className="menu-grid">{menu[active].map(([name, description, price]) => <article className="menu-item" key={name}><div><h3>{name}</h3><p>{description}</p></div><b>{price}</b></article>)}</div></div></section>; }
