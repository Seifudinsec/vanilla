'use client';
import { useState, type FormEvent } from 'react'; import Button from './Button';
export default function Reservation() { const [sent, setSent] = useState(false); const submit = (e: FormEvent) => { e.preventDefault(); setSent(true); }; return <section id="reserve" className="section reservation"><div className="reservation-card"><p className="eyebrow">Reservation</p><h2>Keep us a table.</h2><p>We hold every table for fifteen minutes past the hour.</p><form onSubmit={submit}><label>Full name<input required name="name" /></label><label>Phone<input required type="tel" name="phone" /></label><label>Date<input required type="date" name="date" /></label><label>Time<input required type="time" name="time" /></label><label className="full">Party size<input required type="number" min="1" max="12" name="party" /></label><Button solid className="full" type="submit">{sent ? 'Request sent' : 'Confirm reservation'}</Button></form></div></section>; }
