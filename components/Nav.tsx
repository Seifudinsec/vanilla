'use client';
import { useEffect, useState } from 'react';
const links = [['Story', '#story'], ['Menu', '#menu'], ['Gallery', '#gallery'], ['Contact', '#contact']];
export default function Nav() { const [open, setOpen] = useState(false); const [scrolled, setScrolled] = useState(false); useEffect(() => { const fn = () => setScrolled(scrollY > 60); addEventListener('scroll', fn); fn(); return () => removeEventListener('scroll', fn); }, []); return <header className={scrolled ? 'nav scrolled' : 'nav'}><a href="#top" className="brand"><img src="/logo.png" alt="Anastasia Logo" className="logo-img nav-logo-img" /></a><button className="nav-toggle" onClick={() => setOpen(!open)} aria-label="Toggle navigation">{open ? '×' : '☰'}</button><nav className={open ? 'nav-links open' : 'nav-links'}>{links.map(([label, href]) => <a onClick={() => setOpen(false)} href={href} key={href}>{label}</a>)}<a onClick={() => setOpen(false)} className="reserve-link" href="#reserve">Reserve a table</a></nav></header>; }
