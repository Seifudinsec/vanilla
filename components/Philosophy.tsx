import Reveal from './Reveal';
const steps = [['01', 'Shade-grown tencha', 'Our matcha leaves grow under native canopy for twenty days before harvest — a choice that costs more and tastes like it.'], ['02', 'Selected by leaf grade', 'Every lot is sorted by shade depth and tenderness. Only the youngest leaves make it into the hopper.'], ['03', 'Stone-milled to order', 'Ground on granite, a few grams at a time, so the color stays vivid and the flavor stays alive.'], ['04', 'Muddled to order', 'No pre-mix, no batch syrup sitting on a shelf. Your mojito starts when you sit down, not before.']];
export default function Philosophy() { return <section className="section philosophy"><div className="container narrow"><Reveal><div className="section-head"><p className="eyebrow">The vanilla way</p><h2>From leaf to <em>cup</em>, nothing rushed.</h2></div></Reveal>{steps.map(([n, title, copy]) => <Reveal key={n}><article className="philosophy-row"><i>{n}</i><div><h3>{title}</h3><p>{copy}</p></div></article></Reveal>)}</div></section>; }
