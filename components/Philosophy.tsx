import Reveal from './Reveal';
const steps = [['01', 'Grown in shade', 'Our farms grow under native canopy, which slows ripening and deepens flavor — a choice that costs more and tastes like it.'], ['02', 'Cupped before it’s chosen', 'Every lot is tasted blind by three roasters. Ties are broken by the person who’s been here longest.'], ['03', 'Roasted in small batches', 'Twelve kilos at a time, so the drum never has to compromise on any single bean’s schedule.'], ['04', 'Poured to order', 'No urns, no batch brew sitting on a burner. Your cup starts when you sit down, not before.']];
export default function Philosophy() { return <section className="section philosophy"><div className="container narrow"><Reveal><div className="section-head"><p className="eyebrow">The vanilla way</p><h2>From cherry to <em>cup</em>, nothing rushed.</h2></div></Reveal>{steps.map(([n, title, copy]) => <Reveal key={n}><article className="philosophy-row"><i>{n}</i><div><h3>{title}</h3><p>{copy}</p></div></article></Reveal>)}</div></section>; }
