import { signatureDrinks } from '@/data/site'; import Reveal from './Reveal';
export default function Coffee() { return <section className="section coffee"><div className="container"><Reveal><div className="section-head"><p className="eyebrow">The signature</p><h2>Five drinks we&apos;d defend <em>with our lives.</em></h2></div></Reveal><div className="coffee-list">{signatureDrinks.map(([number, name, description, origin, price]) => <Reveal key={name}><article className="coffee-row"><span className="coffee-number">{number}</span><span className="coffee-icon">◌</span><h3>{name}</h3><p>{description}</p><small>{origin}</small><b>{price}</b></article></Reveal>)}</div></div></section>; }
