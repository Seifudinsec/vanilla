import Reveal from './Reveal';
const testimonials = [['“The first place I’ve been where the barista could tell me which hillside my coffee grew on.”', 'Amara O.', 'Regular since 2019'], ['“I come for the arch window and the quiet. I stay because the espresso is the best in the city.”', 'Daniel K.', 'Weekly visitor'], ['“Vanilla treats a pastry order the way other places treat a tasting menu.”', 'Leila M.', 'Food writer']];
export default function Testimonials() { return <section className="section testimonials"><div className="container"><Reveal><div className="section-head"><p className="eyebrow">From the regulars</p><h2>Words from the <em>quiet corner.</em></h2></div></Reveal><div className="testimonial-grid">{testimonials.map(([quote, name, role]) => <Reveal key={name}><article><blockquote>{quote}</blockquote><b>{name}</b><small>{role}</small></article></Reveal>)}</div></div></section>; }
